# To execute the code, please follow the steps below:

1. create a virtual environment and install the dependencies from the `requirements.txt` file
2. install the spacy model for spanish language: `python -m spacy download es_core_news_sm`
3. run the `main.py` file

